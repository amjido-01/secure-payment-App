var typed = new Typed('.element', {
    strings: ['gen x', 'era', 'Generation'],
    typeSpeed: 200,
    backSpeed: 100,
    loop:true
});

var typed = new Typed('.move', {
    strings: ['One Click'],
    typeSpeed: 200,
    backSpeed: 100,
    loop:true
});

var typed = new Typed('.why', {
    strings: ['Banksecure?'],
    typeSpeed: 200,
    backSpeed: 100,
    loop:true
})
